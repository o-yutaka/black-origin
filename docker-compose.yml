version: "3.9"
services:
  black-origin:
    image: python:3.12-slim
    working_dir: /app
    volumes:
      - ./:/app
    command: bash -lc "pip install -r requirements.txt && python run.py"
    env_file:
      - .env
    ports:
      - "8080:8080"
